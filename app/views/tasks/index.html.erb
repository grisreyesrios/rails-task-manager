<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %> <%= link_to "Edit", edit_task_path(task) %> <%= link_to "Delete", task_path(task), method: :delete , data: { confirm: "Do you really want to delete?" } %>
    </li>
  <% end %>
</ul>

<br>

<%= link_to "New task", new_task_path %>

