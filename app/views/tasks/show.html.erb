<h1><%=  @task.title %></h1>
<p><%= @task.details %></p>

<br>

<%= link_to "Return to tasks list", tasks_path %> <%= link_to "Edit", edit_task_path(@task) %> <%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Really delete it?"} %>

